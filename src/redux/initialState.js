import userState from './initial_state/user';
import jogState from './initial_state/jogs';
import filterState from './initial_state/filter';

export const user = userState;
export const jog = jogState;
export const filter = filterState;

export default {
    user,
    jog,
    filter
};
